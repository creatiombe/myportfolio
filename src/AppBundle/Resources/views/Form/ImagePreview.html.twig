{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field %}
    <div>
        {% if object.imageName != null %}
            <img src="{{ object.imageName }}">
        {% else %}
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}